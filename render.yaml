services:
  - type: web
    name: monitoreo-paramos
    runtime: python
    buildCommand: bash build.sh
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 2
    envVars:
      - key: SECRET_KEY
        generateValue: true